apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "koreader-sync.fullname" . }}-test-connection"
  labels:
    {{- include "koreader-sync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "koreader-sync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
