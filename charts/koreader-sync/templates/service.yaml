apiVersion: v1
kind: Service
metadata:
  name: {{ include "koreader-sync.fullname" . }}
  labels:
    {{- include "koreader-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "koreader-sync.selectorLabels" . | nindent 4 }}
