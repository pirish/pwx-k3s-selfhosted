---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "koreader-sync.fullname" . }}-env
  labels:
    {{- include "koreader-sync.labels" . | nindent 4 }}
data:
  RECEIVE_RANDOM_DEVICE_ID: "{{ .Values.koreader-sync.config.receiveRandomDeviceId }}"
  OPEN_REGISTRATIONS: "{{ .Values.koreader-sync.config.openRegistrations }}"