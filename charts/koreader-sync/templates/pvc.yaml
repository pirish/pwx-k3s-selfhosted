---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "koreader-sync.fullname" . }}-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 500Mi