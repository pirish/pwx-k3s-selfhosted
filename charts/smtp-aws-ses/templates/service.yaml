apiVersion: v1
kind: Service
metadata:
  name: {{ include "smtp-aws-ses.fullname" . }}
  labels:
    {{- include "smtp-aws-ses.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: smtp
      protocol: TCP
      name: smtp
  selector:
    {{- include "smtp-aws-ses.selectorLabels" . | nindent 4 }}
