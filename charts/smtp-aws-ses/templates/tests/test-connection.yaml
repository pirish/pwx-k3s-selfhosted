apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smtp-aws-ses.fullname" . }}-test-connection"
  labels:
    {{- include "smtp-aws-ses.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smtp-aws-ses.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
