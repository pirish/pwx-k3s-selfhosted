---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "shelfarr-app.fullname" . }}-config-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 500Mi