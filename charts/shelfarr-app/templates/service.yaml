apiVersion: v1
kind: Service
metadata:
  name: {{ include "shelfarr-app.fullname" . }}-service
  labels:
    {{- include "shelfarr-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shelfarr-app.selectorLabels" . | nindent 4 }}
