apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "copyparty.fullname" . }}-test-connection"
  labels:
    {{- include "copyparty.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "copyparty.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
