apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cloudflared.fullname" . }}-env-config
  labels:
    {{- include "cloudflared.labels" . | nindent 4 }}
data:
  TUNNEL_METRICS: "{{ .Values.CFConfig.metricsCidr }}:{{ .Values.service.port }}"

